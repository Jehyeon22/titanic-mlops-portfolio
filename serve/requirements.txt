# 서빙 이미지(serve/Dockerfile)에 설치되는 패키지만 포함.
# 버전을 == 으로 고정: model.joblib은 피클이라 학습 때와 라이브러리 버전이
# 다르면 InconsistentVersionWarning이 뜨고, 최악엔 로드 자체가 깨진다.
fastapi==0.141.1
uvicorn[standard]==0.52.1
scikit-learn==1.9.0
pandas==3.0.5
numpy==2.5.1
scipy==1.18.0
joblib==1.5.3
prometheus-fastapi-instrumentator==8.1.0
bentoml==1.4.39
