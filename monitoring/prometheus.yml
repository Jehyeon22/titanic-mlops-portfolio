global:
  scrape_interval: 5s

scrape_configs:
  - job_name: titanic-api
    static_configs:
      # serve/docker-compose.yml 로 api+prometheus를 같은 네트워크에서 띄우면
      # 서비스명(api)으로 접근한다. api를 호스트에서 직접 띄웠다면
      # host.docker.internal:8000 으로 바꿀 것.
      - targets: ["api:8000"]
